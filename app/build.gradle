apply plugin: 'com.android.application'

android {
    compileSdkVersion 11
    buildToolsVersion "23.0.2"

    defaultConfig {
        applicationId "net.assemble.yclock"
        minSdkVersion 2
        targetSdkVersion 17
    }

    buildTypes {
        release {
            minifyEnabled true
            proguardFiles getDefaultProguardFile('proguard-android.txt'), 'proguard-project.txt'
        }
    }
}
